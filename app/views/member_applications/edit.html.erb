<% provide(:title, "Edit Membership Application Form") %>

<%= render partial: 'navbar' %>

<%= render partial: 'header' %>

<%= render partial: 'form', locals: { member_application: member_application } %>
